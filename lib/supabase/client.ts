// Legacy Supabase client helper — replaced by Firebase SDK
// Stub kept for type compatibility only
export function createClient() {
  throw new Error('Use Firebase SDK instead of Supabase client')
}
